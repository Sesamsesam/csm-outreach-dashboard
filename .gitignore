# Personal data — never commit
csm_jobs.csv
seen_job_ids.txt
user_profile.txt
cover_letters/*.txt
cover_letters/*.md

# API keys
dashboard/.hunter_key
dashboard/.apollo_key

# Python
__pycache__/
*.pyc
*.pyo

# macOS
.DS_Store

# Generated skill bundles (the .claude/skills/ folders are authoritative;
# run build_skills.sh to produce a .skill zip on demand for Cowork/Desktop)
*.skill

# Temp / unknown files
ziD6IQFX

# Claude personal settings (keep launch.json but not local permissions)
.claude/settings.local.json
